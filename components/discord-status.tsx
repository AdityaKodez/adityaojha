{
  activity.details && (
    <p className="text-[11px] opacity-85 truncate">{activity.details}</p>
  );
}
{
  activity.state && (
    <p className="text-[11px] opacity-85 truncate">{activity.state}</p>
  );
}
